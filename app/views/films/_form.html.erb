  <div class="call-to-action">
<br /> <%= simple_nested_form_for @film, :html => {:multipart => true} do |f| %>
<% if @film.errors.any? %>
        <div>
        <h2><%= pluralize(@film.errors.count, "error") %> prohibited this film from being saved:</h2>

        <ul>
            <% @film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
            <% end %>
        </ul>
        </div>
  <% end %>       
<div class="container">
      <div class="row">
        <div class="col-md-6 col-md-offset-3 text-center">
          <div >
            <%= f.label "D/S/M/A/B/W" %><br>
            <%= f.input :name %>
          </div>
          
          <div >
            <%= f.label "Just the Vimeo Film-ID" %><br>
            <%= f.input :url %>
          </div>
          <div >
            <%= f.label "Featured film?" %><br>
            <%= f.check_box :feature %>
          </div>
          
          
          <div><h1> Add still images</h1>
            <p>The last still added, becomes the poster</p> 
            <%= f.simple_fields_for :stills,
            @film.stills_for_form do |s| %>
             
              <%= s.input :film_id, input_html: {value: @film.id}, as: :hidden %>
              <%= s.file_field :image %>
          
              <%= s.link_to_remove "Remove this still" %>
              
            <% end %>
            <%#= file_field_tag "images[]", type: :file, multiple: true %>
            <%= f.link_to_add "Add a still", :stills %>
          </div>
        <div class="actions">
<%= f.submit  'Submit', :class => "btn btn-lg btn-primary" %>        
        </div> 
        <div >
            <%= f.label "Not needed" %><br>
            <%= f.input :writeup %>
          </div>
          <div >
            <%= f.label "Not needed" %><br>
            <%= f.input :credits %>
          </div>
          <div >
            <%= f.label "Not needed" %><br>
            <%= f.input :link%>
          </div>
        </div> 
      
               
      </div>

<% end %> 
</div>
</div>
  
