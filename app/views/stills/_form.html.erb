<%= simple_form_for(@still) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :film_id %>
    <%= f.input :image %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
